// src/Example.js
import React, { useState } from 'react';
import Layout from '../../components/Layout';

const Home = () => {
  
  return (
    <div style={{ display: 'flex' }}>
        <Layout/>
    </div>
  );
};

export default Home;
